info:
  name: Get Student By Id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/student/2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Retrieve a student by ID. Returns 404 if not found.
